/* eslint-disable prettier/prettier */
export enum RequestStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
  IN_TRANSIT = 'in_transit',
  COMPLETED = 'completed',
  EXPIRED = 'expired',
} 