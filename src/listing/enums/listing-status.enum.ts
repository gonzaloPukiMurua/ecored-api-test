/* eslint-disable prettier/prettier */
export enum ListingStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  RESERVED = 'reserved',
  COMMITTED = 'committed',
  IN_TRANSIT = 'in_transit',
  DELIVERED = 'delivered',
  PAUSED = 'paused',
  CANCELLED = 'cancelled',
  BLOCKED = 'blocked',
  EXPIRED = 'expired',
} 
